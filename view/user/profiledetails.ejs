<!DOCTYPE html>
<html lang="en" class="h-full">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>User Profile Form</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    .form-label {
      @apply block text-gray-700 font-bold mb-2;
    }

    .form-control {
      @apply shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline;
    }

    .form-check-input {
      @apply mr-2;
    }

    .btn-primary {
      @apply bg-[#81c408] hover:bg-[#6aae06] text-white font-bold py-2 px-4 rounded;
    }
  </style>
</head>
<body class="h-full flex justify-center items-center bg-gray-100">
  <form action="/profiledetails" method="post">
  <div class="max-w-md mx-auto bg-white rounded-xl shadow-md overflow-hidden">
    <div class="px-8 py-6">
      <h2 class="text-2xl font-bold text-gray-800 mb-6">User Profile</h2>
      <form id="user-profile-form" action="/userdetails" method="post">
        <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
          <div>
            <label class="form-label" for="username">First Name<sup>*</sup></label>
            <input type="text" class="form-control" name="username" required>
          </div>
          <div>
            <label class="form-label" for="userlastname">Last Name<sup>*</sup></label>
            <input type="text" class="form-control" name="userlastname" required>
          </div>
        </div>
        <div class="mt-4">
          <label class="form-label" for="dob">DOB<sup>*</sup></label>
          <input type="date" class="form-control" name="dob" required>
        </div>
        <div class="mt-4">
          <label class="form-label">Gender</label>
          <div class="flex items-center">
            <div class="form-check">
              <input class="form-check-input" type="radio" name="gender" id="male" value="Male" required>
              <label class="form-check-label" for="male">Male</label>
            </div>
            <div class="form-check ml-4">
              <input class="form-check-input" type="radio" name="gender" id="female" value="Female">
              <label class="form-check-label" for="female">Female</label>
            </div>
            <div class="form-check ml-4">
              <input class="form-check-input" type="radio" name="gender" id="other" value="Other">
              <label class="form-check-label" for="other">Other</label>
            </div>
          </div>
        </div>
        <div class="mt-4">
          <label class="form-label" for="mobile">Mobile<sup>*</sup></label>
          <input type="tel" class="form-control" name="mobile" required>
        </div>
        <hr class="my-6">
        <button type="submit" class="btn-primary">Submit</button>
      </form>
    </div>
  </div>
</form>

  <script>
    const form = document.getElementById('user-profile-form');
    form.addEventListener('submit', function(event) {
      event.preventDefault();
      // Prevent default form submission
      // Here, you can handle form submission logic
      // For example, you could validate the form data or send it to the server
      console.log('Form submitted');
    });
  </script>
</body>
</html
